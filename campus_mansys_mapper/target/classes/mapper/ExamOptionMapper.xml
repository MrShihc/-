<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mapper.ExamOptionMapper">

    <!-- 保存试卷选项信息 -->
    <insert id="saveExamOptionInfo">
        INSERT INTO `tb_examoption`(`oname`, `istrue`, `examid`) VALUES (#{oname},#{istrue},#{examid});
    </insert>

</mapper>